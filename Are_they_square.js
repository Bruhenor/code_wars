/*Write a function that checks whether all elements in an array are square numbers. The function should be able to take any number of array elements.

Your function should return true if all elements in the array are square numbers and false if not.

An empty array should return undefined / None / nil /false (for C). You can assume that all array elements will be positive integers.

Examples:

is_square([1, 4, 9, 16]) --> True

is_square([3, 4, 7, 9]) --> False

is_square([]) --> None

*/


/*

Pszeudokód

1.)  Bemeneti feltételek megadás
2.)  FOR Loop létrehozása, if, else if, else, kondiciók helyes megadása ( Ha egy szám oszható önmafával maradék nélkül akkor )
3.) A az újjonan keletkezett arrayEven és arrayOdd tagjait külön külön összegezzük.
4.) A két array végeeredményét console.log-gal megjelenítjük

*/

let isSquare = function(arr){
    
    let = team;
    let = answer

    for (let i = 0; i < array.length; i++) {
       
        if team  = Math.SQRT2
          
    ];
        
    }

    return = answer;
  }


  function getRoot2() {
    return Math.SQRT2;
  }
  